# Pond Lab UI

`PondLibrary.jsx` presents reusable pieces. `PondExperiment.jsx` composes those pieces as a single interactive scene. Keep this documentation with the feature and keep route components focused on presentation.
