import { LockIcon } from './LockIcon';

export const metadata = {
  title: "Unlock Animation - UiGlow",
  description: "SVG lock and unlock animation with smooth morphing transitions.",
};

export default function Unlock() {
  return (
    <div>
      <LockIcon />
    </div>
  );
}