# Pond Lab UI

`PondLibrary.jsx` presents reusable pieces. `PondExperiment.jsx` composes those pieces as a single interactive scene. Keep documentation in `src/lib/pond-lab/README.md` and keep these route components focused on presentation.
