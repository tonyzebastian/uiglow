import ExploreClient from './ExploreClient';

export const metadata = {
  title: "Explore UI Interactions - UiGlow",
  description: "Browse and explore all UI interaction components.",
};

export default function ExploreUIInteractionsPage() {
  return <ExploreClient />;
}
